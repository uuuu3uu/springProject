<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.green2209S_14.dao.AdminDAO">
	
	<update id="setMemberLevelCheck">
		update member2 set level = #{level} where idx = #{idx};
	</update>
	
	<update id="setbookingStatusCheck">
		update booking2 set bookingStatus = #{bookingStatus} where idx = #{idx};
	</update>
	
	
</mapper>